product: '1.0.0'

info:
  name: cryptoment-loopback
  title: cryptoment-loopback
  version: "1.0.0"

apis:
  'cryptoment-loopback':
    $ref: cryptoment-loopback.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
